<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta name="description" content="TNH Store " />
    <meta name="author" content="Hisuirika" />
    <title>Manager User</title>
    <link href="https://cdn.jsdelivr.net/npm/simple-datatables@7.1.2/dist/style.min.css" rel="stylesheet" />
    <link href="/admin/css/styles.css" rel="stylesheet" />
    <script src="https://use.fontawesome.com/releases/v6.3.0/js/all.js" crossorigin="anonymous"></script>
</head>

<body class="sb-nav-fixed">
    <%- include('../layout/header'); %>

        <div id="layoutSidenav">
            <%- include('../layout/sidenav'); %>

                <div id="layoutSidenav_content">
                    <main>
                        <div class="container-fluid px-4">
                            <h1 class="mt-4">Manage Users</h1>
                            <ol class="breadcrumb mb-4">
                                <li class="breadcrumb-item"><a href="/admin">Dashboard</a></li>
                                <li class="breadcrumb-item active">Users</li>
                            </ol>
                            <div style="display: flex; justify-content: space-between;">
                                <h5> List Users</h5>
                                <a href=" /admin/creatUser" class="btn btn-warning">Create User</a>
                            </div>
                            <hr />
                            <table class="table table-hover">
                                <thead>
                                    <tr>
                                        <th>ID</th>
                                        <th>Full Name</th>
                                        <th>Email</th>
                                        <th>Address</th>
                                        <th>Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% users.forEach(function(user) { %>
                                        <tr>
                                            <th>
                                                <%= user.id %>
                                            </th>
                                            <td>
                                                <%= user.name %>

                                            </td>
                                            <td>
                                                <%= user.email %>
                                            </td>
                                            <td>
                                                <%= user.address %>
                                            </td>
                                            <td style=" display: flex;gap:10px">
                                                <form action="/admin/delete-user/<%= user.id %>" method="post">
                                                    <button class="btn btn-danger">Delete</button>
                                                </form>
                                                <form action="/admin/view-user/<%= user.id %>" method="get">
                                                    <button class="btn btn-warning">View</button>
                                                </form>
                                            </td>
                                        </tr>
                                        <% }); %>

                                </tbody>
                            </table>
                            <nav aria-label="Page navigation example">
                                <ul class="pagination justify-content-center">
                                    <li class="page-item">
                                        <a class="<%= page === 1 ? 'disabled page-link' : 'page-link' %>"
                                            href="/admin/userPage?page=<%= page - 1  %>" aria-label="Previous">
                                            <span aria-hidden="true">&laquo;</span>
                                        </a>
                                    </li>
                                    <% for(var i=1; i <=totalPages; i++) {%>
                                        <li class="page-item">
                                            <a class="<%= page === i ? 'active page-link' : 'page-link' %>"
                                                href="/admin/userPage?page=<%= i  %>">
                                                <%= i %>
                                            </a>
                                        </li>
                                        <% } %>

                                            <li class="page-item">
                                                <a class="<%= page === totalPages ? 'disabled page-link' : 'page-link' %>"
                                                    href="/admin/userPage?page=<%= page + 1  %>" aria-label="Next">
                                                    <span aria-hidden="true">&raquo;</span>
                                                </a>
                                            </li>
                                </ul>
                            </nav>

                        </div>

                    </main>
                    <%- include('../layout/footer'); %>

                </div>
        </div>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"
            crossorigin="anonymous"></script>
        <script src="/admin/js/scripts.js"></script>

</body>

</html>